{
  "name": "batch-poster-monitor",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "lint": "eslint .",
    "build": "rm -rf ./dist && tsc",
    "format": "prettier './**/*.{js,json,md,yml,sol,ts}' --write && yarn run lint --fix",
    "dev": "yarn build && node ./dist/batch-poster-monitor/index.js"
  },
  "dependencies": {
    "@arbitrum/orbit-sdk": "^0.23.1",
    "abitype": "^1.0.5",
    "typescript": "^5.4.5",
    "utils": "*",
    "viem": "1.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.1",
    "ts-node": "^10.9.2"
  }
}
